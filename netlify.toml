[build]
  # Ensures node_modules exists for Netlify Functions when deploying from GitHub
  command = "npm install"
  publish = "."
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "20"

# Mongoose: NFT trace is more reliable than default esbuild for some native deps
[functions]
  node_bundler = "nft"
